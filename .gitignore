node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Replit files
.local/
.replit

# Environment variables
.env
.env.local
.env.*.local

# Database backups (may contain secrets/PII — session tokens, Google OAuth tokens, password hashes)
attached_assets/DB Backup/
attached_assets/server_backups/
attached_assets/*.dump
attached_assets/*.sql
attached_assets/neon_backup.sql
backups/
restore.log

# Google Service Account credentials
server/credentials.json
zeta-courage-*.json
*-credentials.json
*-service-account.json
*.json
!package.json
!package-lock.json
!tsconfig.json
!components.json
